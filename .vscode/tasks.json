{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "start",
            "type": "shell",
            "command": "${config:python.pythonPath} start.py --2d-plot",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}